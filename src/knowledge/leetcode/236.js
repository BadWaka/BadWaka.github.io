
import {
    TreeNode,
    genBinaryTree,
} from '../data-structure/tree/tree.js';
console.log('TreeNode', TreeNode, 'genBinaryTree', genBinaryTree);

/**
 * @returns
 */
function f(nums) {
}

let params = '';
const res = f(params);
console.log('\nres', res);
